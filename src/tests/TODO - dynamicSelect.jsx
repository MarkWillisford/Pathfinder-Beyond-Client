import React from 'react';
import {shallow} from 'enzyme';
import {DynamicSelect} from '../components/dynamicSelect';

/* describe('<DynamicSelect />', () => {
  it('Renders without crashing', () => {
    shallow(<DynamicSelect />);
  });
}) */