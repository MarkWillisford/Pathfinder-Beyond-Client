import React from 'react';
import {shallow} from 'enzyme';
import {CharacterReview} from '../components/characterReview2';

/* describe('<CharacterReview />', () => {
  const creationSteps = [
    {
      name: 'Character Basics',
      id: 0,
      complete: false
    }
  ]

  it('Renders without crashing', () => {
    // shallow(<CharacterReview creationSteps={creationSteps} characterToReview={newCharacter}/>);
  });
}) */